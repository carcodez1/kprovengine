# .env.example — copy this to `.env` and customize

# Path to your Obsidian vault
OBISIDIAN_VAULT_PATH="/path/to/your/vault"

# Local Vision LLM (Ollama)
LLM_ENDPOINT="http://localhost:11434/api/chat"
LLM_MODEL="qwen2.5vl:3b"

# Python environment
VENV_DIR=".venv"

# Docker vault path
DOCKER_VAULT_PATH="/vault"
