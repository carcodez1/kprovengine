## Purpose

This is an application-level CLI that consumes the `kprovengine` library.

- It is NOT part of the core package
- It is NOT included in the PyPI distribution
- It may depend on optional adapters (OCR, LLM)
- It is intended for local use and experimentation
